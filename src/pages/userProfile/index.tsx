import React from 'react';
import { View, Text } from 'react-native';

const UserProfile = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>User Profile Page</Text>
    </View>
  );
};

export default UserProfile;
