import { View, Text } from 'react-native';

const MealPlans = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>Meal Plans Page</Text>
    </View>
  );
};

export default MealPlans;
